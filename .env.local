# Local Development Environment

# Frontend URL (local dev server)
BASE_URL=http://localhost:5173

# Backend API URL (local backend)
API_BASE_URL=http://localhost:3000/api/v1

# Browser Settings (show browser for debugging)
HEADLESS=false
BROWSER=chromium
SLOW_MO=100
DEVICE=desktop

# Test Execution (fewer workers for local)
PYTEST_WORKERS=4
